[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
# Make `config/` a Python root so tests can do:
#   from custom_components.claude_usage.coordinator import ...
pythonpath = config
